## Change

Describe the trigger and resulting behavior.

## Validation

- [ ] `uv run pytest -q`
- [ ] `uv run ruff check src tests`
- [ ] `cd web && pnpm build` (if web changed)
- [ ] No source conversations, credentials, or local database files included
